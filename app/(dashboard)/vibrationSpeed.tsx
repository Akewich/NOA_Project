import { StyleSheet, Text, View } from "react-native";
import React from "react";

const VibrationSpeedScreen = () => {
  return (
    <View>
      <Text>VibrationSpeed</Text>
    </View>
  );
};

export default VibrationSpeedScreen;

const styles = StyleSheet.create({});
